# Docs

Architecture and package documentation for SBN Billing will live here as packages are approved and built.

A1.1 (current): project skeleton only — no database, no auth, no healthcare/billing rules.
